#include "colorschemebar.h"

ColorSchemeBar::ColorSchemeBar(QWidget *parent) {
}

ColorSchemeBar::~ColorSchemeBar() {
}

void ColorSchemeBar::setColorScheme(ColorScheme *scheme) {
}

void ColorSchemeBar::paintEvent(QPaintEvent *event) {
    QWidget::paintEvent(event);
}
